/**
 * Placeholder shell — replaced in slice 5 with FluentProvider, theming and Apollo.
 * Exists now so the toolchain (build + tests) has something real to compile.
 */
export function App() {
  return <h1>Axis Camera Manager</h1>;
}
